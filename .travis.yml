language: go

go:
  - 1.x
  - "1.10"
  - 1.11.x
  - master

before_install:
  - cp .netrc ~/.netrc

install:
  - go get github.com/ma-null/net_cli/request
  - go get github.com/urfave/cli

script:
  - go test -v ./...